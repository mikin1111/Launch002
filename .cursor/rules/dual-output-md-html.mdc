---
description: 所有指令输出必须同时生成 Markdown 文件和对应的完整 HTML 文件，确保两者内容、格式、路径完全一致
alwaysApply: true
---

# 双格式输出规则：Markdown + HTML

所有指令的输出必须遵循以下流程：

## 输出顺序

1. **先输出 Markdown 文件**（`.md`）—— 作为内容的主源
2. **再输出对应的 HTML 文件**（`.html`）—— 基于 Markdown 内容生成

## 一致性要求

- **内容一致**：HTML 中的文本、段落、列表、表格、代码块等必须与 Markdown 完全对应，不得遗漏任何内容
- **格式一致**：标题层级、列表嵌套、强调样式、链接、图片路径等必须在 HTML 中正确还原
- **路径一致**：Markdown 和 HTML 文件应位于同一目录，文件名相同（仅扩展名不同），内部引用的资源路径保持一致
- **无样式偏差**：HTML 应包含合理的默认样式，确保视觉呈现与 Markdown 渲染效果一致

## HTML 文件要求

- 必须是完整的 HTML 文档（包含 `<!DOCTYPE html>`、`<html>`、`<head>`、`<body>`）
- `<head>` 中设置 `<meta charset="UTF-8">` 和合适的 `<title>`
- 包含内联 CSS 样式，保证排版美观、可读性好
- 代码块需有语法高亮或至少有 `<pre><code>` 包裹并设定等宽字体和背景色
- 表格、列表、引用块等元素需有适当样式

## 示例

若输出文件为 `docs/guide.md`，则必须同时输出 `docs/guide.html`，两者内容完全对应。
